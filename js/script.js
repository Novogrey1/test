// ============================================
// LANGUAGE SYSTEM - TRANSLATION OBJECT
// ============================================

const translations = {
  "ru": {
    "Назад": "Назад",
    "Главная": "Главная",
    "Команда проекта": "Команда проекта",
    "События и смены": "События и смены",
    "Документация": "Документация",
    "Прочее": "Прочее",
    "Социальные сети": "Социальные сети",
    "TRP RP - RolePlay Project": "TRP RP - RolePlay Project",
    "Первый РП-сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place (Ved's Trolleybuses Place).": "Первый РП-сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place (Ved's Trolleybuses Place).",
    "Сервер Discord": "Сервер Discord",
    "Группа Roblox": "Группа Roblox",
    "Сервер Roblox": "Сервер Roblox",
    "TRP RP": "TRP RP",
    "Чтобы удобно пользоваться этим сайтом, включите режим «Версия для ПК» и переверните телефон в горизонтальное положение.": "Чтобы удобно пользоваться этим сайтом, включите режим «Версия для ПК» и переверните телефон в горизонтальное положение.",
    "TRP RP: Реалистичный РП-проект на основе OneSkyVed's Trolleybuses": "TRP RP: Реалистичный РП-проект на основе OneSkyVed's Trolleybuses",
    "\"TRP RP\" — это игровой РП-проект и первый сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place. Проект предоставляет уникальный игровой опыт, качественное общение, атмосферу, возможности карьерного роста и незабываемые эмоции! На сервере множество должностей различных категорий, и мест всегда достаточно для работы на нашем проекте!": "\"TRP RP\" — это игровой РП-проект и первый сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place. Проект предоставляет уникальный игровой опыт, качественное общение, атмосферу, возможности карьерного роста и незабываемые эмоции! На сервере множество должностей различных категорий, и мест всегда достаточно для работы на нашем проекте!",
    "Главный администратор ОСРП": "Главный администратор ОСРП",
    "Зам. главного ОСРП Начало": "Зам. главного ОСРП Начало",
    "Ведущие ОСОФ": "Ведущие ОСОФ",
    "Команда и Лидеры": "Команда и Лидеры",
    "Прозрачная система правил!": "Прозрачная система правил!",
    "Мы стремимся к созданию комфортной и дружелюбной среды для всех участников нашего сообщества. Поэтому на нашем сервере действует система прозрачных правил, которые понятны каждому.": "Мы стремимся к созданию комфортной и дружелюбной среды для всех участников нашего сообщества. Поэтому на нашем сервере действует система прозрачных правил, которые понятны каждому.",
    "Обеспечение хорошего карьерного роста!": "Обеспечение хорошего карьерного роста!",
    "На нашем сервере мы создаем условия, которые помогут каждому участнику расти, развиваться и достигать новых высот.": "На нашем сервере мы создаем условия, которые помогут каждому участнику расти, развиваться и достигать новых высот.",
    "Качественная организация мероприятий!": "Качественная организация мероприятий!",
    "Мы придаем большое значение созданию уникальных и интересных мероприятий, которые объединяют наше сообщество и делают времяпрепровождение незабываемым.": "Мы придаем большое значение созданию уникальных и интересных мероприятий, которые объединяют наше сообщество и делают времяпрепровождение незабываемым.",
    "Приятное времяпрепровождение!": "Приятное времяпрепровождение!",
    "Наш сервер создан для того, чтобы каждый участник чувствовал себя комфортно, расслабленно и получал удовольствие от общения.": "Наш сервер создан для того, чтобы каждый участник чувствовал себя комфортно, расслабленно и получал удовольствие от общения.",
    "Контакты проекта \"TRP RP\"": "Контакты проекта \"TRP RP\"",
    "Discord": "Discord",
    "TeamSpeak": "TeamSpeak",
    "YouTube": "YouTube",
    "TikTok": "TikTok",
    "Telegram": "Telegram",
    "ВКонтакте": "ВКонтакте",
    "Юридическая документация проекта \"TRP RP\"": "Юридическая документация проекта \"TRP RP\"",
    "Политика конфиденциальности": "Политика конфиденциальности",
    "Пользовательское соглашение": "Пользовательское соглашение",
    "Проект \"TRP RP\" не является юридическим лицом и не предоставляет никаких услуг!": "Проект \"TRP RP\" не является юридическим лицом и не предоставляет никаких услуг!",
    "© 2026 TRP RP. Все права защищены.": "© 2026 TRP RP. Все права защищены."
  },
  "en": {
    "Назад": "Back",
    "Главная": "Home",
    "Команда проекта": "Project Team",
    "События и смены": "Events and Shifts",
    "Документация": "Documentation",
    "Прочее": "Other",
    "Социальные сети": "Social Media",
    "TRP RP - RolePlay Project": "TRP RP - RolePlay Project",
    "Первый РП-сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place (Ved's Trolleybuses Place).": "The first RP server based on the Roblox place: OneSkyVed's Trolleybuses Place (Ved's Trolleybuses Place).",
    "Сервер Discord": "Discord Server",
    "Группа Roblox": "Roblox Group",
    "Сервер Roblox": "Roblox Server",
    "TRP RP": "TRP RP",
    "Чтобы удобно пользоваться этим сайтом, включите режим «Версия для ПК» и переверните телефон в горизонтальное положение.": "For convenient use of this site, enable \"Desktop version\" mode and rotate your phone to landscape orientation.",
    "TRP RP: Реалистичный РП-проект на основе OneSkyVed's Trolleybuses": "TRP RP: Realistic RP Project Based on OneSkyVed's Trolleybuses",
    "Главный администратор ОСРП": "Chief Administrator OSRP",
    "\"TRP RP\" — это игровой РП-проект и первый сервер, основанный на плейсе Roblox: OneSkyVed's Trolleybuses Place. Проект предоставляет уникальный игровой опыт, качественное общение, атмосферу, возможности карьерного роста и незабываемые эмоции! На сервере множество должностей различных категорий, и мест всегда достаточно для работы на нашем проекте!": "\"TRP RP\" is a gaming RP project and the first server based on the Roblox place: OneSkyVed's Trolleybuses Place. The project provides a unique gaming experience, quality communication, atmosphere, career growth opportunities, and unforgettable emotions! The server has many positions of various categories, and there are always enough places to work on our project!",
    "Зам. главного ОСРП Начало": "Deputy Chief OSRP Beginning",
    "Ведущие ОСОФ": "OSOF Hosts",
    "Команда и Лидеры": "Team and Leaders",
    "Прозрачная система правил!": "Transparent Rule System!",
    "Мы стремимся к созданию комфортной и дружелюбной среды для всех участников нашего сообщества. Поэтому на нашем сервере действует система прозрачных правил, которые понятны каждому.": "We strive to create a comfortable and friendly environment for all participants in our community. Therefore, our server has a system of transparent rules that are clear to everyone.",
    "Обеспечение хорошего карьерного роста!": "Ensuring Good Career Growth!",
    "На нашем сервере мы создаем условия, которые помогут каждому участнику расти, развиваться и достигать новых высот.": "On our server, we create conditions that will help every participant grow, develop and reach new heights.",
    "Качественная организация мероприятий!": "High-Quality Event Organization!",
    "Мы придаем большое значение созданию уникальных и интересных мероприятий, которые объединяют наше сообщество и делают времяпрепровождение незабываемым.": "We attach great importance to creating unique and interesting events that unite our community and make leisure time unforgettable.",
    "Приятное времяпрепровождение!": "Pleasant Leisure!",
    "Наш сервер создан для того, чтобы каждый участник чувствовал себя комфортно, расслабленно и получал удовольствие от общения.": "Our server is designed so that every participant feels comfortable, relaxed and enjoys the communication.",
    "Контакты проекта \"TRP RP\"": "TRP RP Project Contacts",
    "Discord": "Discord",
    "TeamSpeak": "TeamSpeak",
    "YouTube": "YouTube",
    "TikTok": "TikTok",
    "Telegram": "Telegram",
    "ВКонтакте": "VKontakte",
    "Юридическая документация проекта \"TRP RP\"": "Legal Documentation of the TRP RP Project",
    "Политика конфиденциальности": "Privacy Policy",
    "Пользовательское соглашение": "Terms of Use",
    "Проект \"TRP RP\" не является юридическим лицом и не предоставляет никаких услуг!": "The \"TRP RP\" project is not a legal entity and does not provide any services!",
    "© 2026 TRP RP. Все права защищены.": "© 2026 TRP RP. All rights reserved."
  }
};

let originalHTML = null;

// ============================================
// THEME SYSTEM
// ============================================

function initThemeSystem() {
    const savedTheme = localStorage.getItem('theme') || 'light';
    applyTheme(savedTheme);
    
    const themeBtn = document.getElementById('theme-btn');
    if (themeBtn) {
        themeBtn.addEventListener('click', handleThemeClick);
    }
}

function applyTheme(theme) {
    localStorage.setItem('theme', theme);
    const themeBtn = document.getElementById('theme-btn');
    
    if (theme === 'dark') {
        document.body.classList.add('dark-mode');
        if (themeBtn) themeBtn.textContent = '☀️';
    } else {
        document.body.classList.remove('dark-mode');
        if (themeBtn) themeBtn.textContent = '🌙';
    }
}



async function updateServerButton() {
    try {
        const response = await fetch('https://script.google.com/macros/s/AKfycbylGfVRtvzZI2RMhidNIDhCChNFyFeH7BLyc-PZxTNB35ImcTebTddW1hwq2V2ef5__zw/exec');
        const data = await response.json();
        const status = data.status;

        const serverButton = document.getElementById('server-button');
        if (!serverButton) return;

        if (status === 'Disable RP Server' || status === 'Disable Non-RP Server' || status === 'Disable Shift Server') {
            serverButton.classList.add('red');
            serverButton.classList.remove('green', 'yellow');
        } else if (status === 'Включить RP сервер' || status === 'Включить Non-RP сервер' || status === 'Включить сервер для смен') {
            serverButton.classList.add('green');
            serverButton.classList.remove('red', 'yellow');
        } else if (status === 'Перезагрузка / Restarting') {
            serverButton.classList.add('yellow');
            serverButton.classList.remove('red', 'green');
        }
    } catch (error) {
        console.error('Ошибка при обновлении кнопки сервера:', error);
    }
}

// Запускаем обновление статуса сервера
updateServerButton();
setInterval(updateServerButton, 5000); // Обновляем каждые 5 секунд

// Скрипт для интерактивности сайта

// Навигация - прозрачная вначале, темнеет при скролле
window.addEventListener('scroll', () => {
    const navbar = document.querySelector('.navbar');
    if (window.scrollY > 50) {
        navbar.classList.add('scrolled');
    } else {
        navbar.classList.remove('scrolled');
    }
});

// Плавная прокрутка при клике на навигационные ссылки
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});

// Функция для отслеживания активной ссылки в меню при прокрутке
window.addEventListener('scroll', () => {
    const sections = document.querySelectorAll('section');
    const navLinks = document.querySelectorAll('.nav-link');

    let current = '';
    sections.forEach(section => {
        const sectionTop = section.offsetTop;
        const sectionHeight = section.clientHeight;
        if (window.scrollY >= sectionTop - 200) {
            current = section.getAttribute('id');
        }
    });

    navLinks.forEach(link => {
        link.classList.remove('active');
        if (link.getAttribute('href') === `#${current}`) {
            link.classList.add('active');
        }
    });
});

// Функция для открытия ссылок контактов в новом окне
document.querySelectorAll('.contact-btn, .btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
        // Здесь можно добавить логику для ссылок
        // Например: открыть Discord сервер, группу Roblox и т.д.
        if (this.textContent.includes('Discord')) {
            // e.preventDefault();
            // window.open('https://discord.gg/your-invite-code', '_blank');
        }
    });
});

// Анимация при загрузке страницы
window.addEventListener('load', () => {
    document.body.classList.add('loaded');
});

// Функция для обработки мобильного меню (если нужно)
function handleMobileMenu() {
    const navMenu = document.querySelector('.nav-menu');
    if (navMenu) {
        // Можно добавить функциональность для мобильного меню
    }
}

// Функция для проверки поддержки Intersection Observer для lazy loading изображений
if ('IntersectionObserver' in window) {
    const imageObserver = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const img = entry.target;
                img.src = img.dataset.src || img.src;
                img.classList.add('loaded');
                observer.unobserve(img);
            }
        });
    });

    document.querySelectorAll('img[data-src]').forEach(img => imageObserver.observe(img));
}

// ============================================
// LANGUAGE SYSTEM INITIALIZATION
// ============================================

function initLanguageSystem() {
    if (!originalHTML) {
        originalHTML = document.body.innerHTML;
    }
    
    const savedLanguage = localStorage.getItem('language') || 'ru';
    
    setLanguage(savedLanguage);
    updateLangButton(savedLanguage);
    
    setupLanguageButton();
    reinitializeEventListeners();
    initThemeSystem();
}

// Функция изменения языка
function setLanguage(lang) {
    localStorage.setItem('language', lang);
    document.documentElement.lang = lang;
    
    // Восстанавливаем исходный HTML
    document.body.innerHTML = originalHTML;
    
    // Переводим все текстовые узлы
    function translateNode(node) {
        if (node.nodeType === 3) { // TEXT_NODE
            let text = node.textContent.trim();
            if (text && text.length > 0 && translations[lang] && translations[lang][text]) {
                node.textContent = node.textContent.replace(text, translations[lang][text]);
            }
        } else if (node.nodeType === 1) { // ELEMENT_NODE
            if (node.tagName !== 'SCRIPT') {
                for (let i = 0; i < node.childNodes.length; i++) {
                    translateNode(node.childNodes[i]);
                }
            }
        }
    }
    
    // Начинаем перевод
    for (let i = 0; i < document.body.childNodes.length; i++) {
        translateNode(document.body.childNodes[i]);
    }
    
    reinitializeEventListeners();
    reinitializeTheme();
}

// Функция обновления кнопки языка
function updateLangButton(lang) {
    const langBtn = document.getElementById('lang-btn');
    if (langBtn) {
        langBtn.textContent = lang === 'ru' ? 'EN' : 'RU';
    }
}

// Функция настройки кнопки языка
function setupLanguageButton() {
    const langBtn = document.getElementById('lang-btn');
    if (langBtn) {
        // Удаляем старые обработчики
        const newLangBtn = langBtn.cloneNode(true);
        langBtn.parentNode.replaceChild(newLangBtn, langBtn);
        
        // Добавляем новый обработчик
        const freshLangBtn = document.getElementById('lang-btn');
        freshLangBtn.addEventListener('click', function() {
            const currentLang = localStorage.getItem('language') || 'ru';
            const newLang = currentLang === 'ru' ? 'en' : 'ru';
            setLanguage(newLang);
            updateLangButton(newLang);
        });
    }
}

// Функция переинициализации обработчиков
function reinitializeEventListeners() {
    setupLanguageButton();
    
    // Плавная прокрутка
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
    
    // Навигация активные ссылки
    const sections = document.querySelectorAll('section');
    const navLinks = document.querySelectorAll('.nav-link');
    
    window.addEventListener('scroll', () => {
        let current = '';
        sections.forEach(section => {
            const sectionTop = section.offsetTop;
            if (window.scrollY >= sectionTop - 200) {
                current = section.getAttribute('id');
            }
        });
        
        navLinks.forEach(link => {
            link.classList.remove('active');
            if (link.getAttribute('href') === `#${current}`) {
                link.classList.add('active');
            }
        });
    });
    
    // Переинициализируем мобильное меню
    initMobileMenu();
}

// Переинициализируем тему после смены языка
function reinitializeTheme() {
    const savedTheme = localStorage.getItem('theme') || 'light';
    applyTheme(savedTheme);
    
    const themeBtn = document.getElementById('theme-btn');
    if (themeBtn) {
        themeBtn.removeEventListener('click', handleThemeClick);
        themeBtn.addEventListener('click', handleThemeClick);
    }
}

// Обработчик кнопки темы
function handleThemeClick() {
    const currentTheme = localStorage.getItem('theme') || 'light';
    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
    applyTheme(newTheme);
}

// ============================================
// FORM HANDLING
// ============================================

// ============================================
// MOBILE MENU TOGGLE
// ============================================

function initMobileMenu() {
    const menuToggle = document.getElementById('menu-toggle');
    const navMenu = document.getElementById('nav-menu');
    const navLinks = navMenu ? navMenu.querySelectorAll('.nav-link') : [];

    // Close menu when back arrow is clicked
    const backBtn = document.getElementById('menu-back-btn');
    if (backBtn) {
        backBtn.addEventListener('click', function() {
            const toggle = document.getElementById('menu-toggle');
            if (toggle && navMenu) {
                toggle.classList.remove('active');
                navMenu.classList.remove('active');
                document.body.classList.remove('menu-open');
            }
        });
    }

    // Toggle menu when hamburger is clicked
    if (menuToggle) {
        // Remove old listeners by cloning
        const newToggle = menuToggle.cloneNode(true);
        menuToggle.parentNode.replaceChild(newToggle, menuToggle);
        newToggle.addEventListener('click', function() {
            newToggle.classList.toggle('active');
            navMenu.classList.toggle('active');
            document.body.classList.toggle('menu-open');
        });
    }

    // Close menu when a link is clicked
    navLinks.forEach(link => {
        link.addEventListener('click', function() {
            const toggle = document.getElementById('menu-toggle');
            if (toggle && navMenu) {
                toggle.classList.remove('active');
                navMenu.classList.remove('active');
                document.body.classList.remove('menu-open');
            }
        });
    });

    // Close menu when clicking outside
    document.addEventListener('click', function(event) {
        const toggle = document.getElementById('menu-toggle');
        if (navMenu && toggle && 
            !navMenu.contains(event.target) && 
            !toggle.contains(event.target)) {
            toggle.classList.remove('active');
            navMenu.classList.remove('active');
            document.body.classList.remove('menu-open');
        }
    });

    // Close menu on window resize if screen is large enough
    window.addEventListener('resize', function() {
        if (window.innerWidth > 768) {
            const toggle = document.getElementById('menu-toggle');
            if (toggle && navMenu) {
                toggle.classList.remove('active');
                navMenu.classList.remove('active');
                document.body.classList.remove('menu-open');
            }
        }
    });
}

document.querySelectorAll('form').forEach(form => {
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        console.log('Form submitted');
    });
});

// ============================================
// INITIALIZATION ON LOAD
// ============================================

if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
        initThemeSystem();
        initLanguageSystem();
        initMobileMenu();
    });
} else {
    initThemeSystem();
    initLanguageSystem();
    initMobileMenu();
}

